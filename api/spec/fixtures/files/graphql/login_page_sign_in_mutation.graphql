mutation LoginPageSignInMutation($input: SignInInput!) {
  signIn(input: $input) {
    token
    user {
      id
      email
      profile {
        username
        __typename
      }
      __typename
    }
    __typename
  }
}
