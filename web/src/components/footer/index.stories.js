import React from 'react';
import { Footer } from '.';

const meta = {
  title: 'Main/Footer',
  component: Footer,
};

export default meta;

const Template = args => <Footer {...args} />;

export const Renders = Template.bind({});
